# AGENTS.md

このファイルは、このリポジトリで作業する AI エージェントおよび開発者向けの共通ルールです。
実装、ツール、CI の実態が変わったときは、このファイルも合わせて更新してください。

## プロジェクト概要

Mixup は、アイドルライブにおける MIX の練習を支援するクライアントアプリです。ユーザーは
楽曲を選び、解析結果と MIX 候補を確認し、曲に同期して練習します。

- クライアントは Flutter / Dart で実装します。
- アプリ本体は `apps/`、再利用可能な Dart / Flutter パッケージは `packages/` に置きます。
- `engine/` は既存の音声解析エンジンです。現在は変更対象外であり、クライアントは既存の
  `packages/mixup_ffi` を通じて利用します。
- 自動解析の結果は確定情報として扱わず、信頼度、推薦理由、注意事項をユーザーが確認・修正
  できるようにします。

## リポジトリ構成

ルートの `pubspec.yaml` は Dart workspace の定義元です。workspace に含めるアプリと
パッケージはここで一元管理します。

```text
.
├── apps/                    # 実行可能な Dart / Flutter クライアント
│   └── mixup_cli/           # engine の動作確認用 Dart CLI
├── packages/
│   └── mixup_ffi/           # Rust engine の Dart / Flutter 向け境界
├── engine/                  # 既存 Rust 音声解析エンジン（当面変更しない）
├── mixs/                    # MIX のナレッジデータ
├── docs/                    # 設計・仕様ドキュメント
└── pubspec.yaml             # Dart workspace と共通開発依存
```

- `apps/` のアプリは `packages/` の公開 API に依存します。アプリ間で実装コードを直接共有
  しません。
- `packages/` はアプリに依存させません。package 間の依存は必要最小限かつ一方向に保ち、循環
  依存を作りません。
- 各 package の公開入口は `lib/<package_name>.dart` です。他の package から `lib/src/` を
  直接 import しません。
- 新しいアプリや package は、実装を開始するタイミングで workspace として追加します。将来の
  ためだけの空ディレクトリや雛形は作成しません。

## 設計原則

Rust・Flutterを問わず、**SSOT**と**SOLID原則**に従って実装します。

### SSOT（Single Source of Truth）

- 仕様・型・定数・ビジネスルールは一箇所を正とし、他の場所へ複製しません。
- 共有スキーマは`contracts/`を正とし、Rust・Flutter・CLIはここから生成または参照します。
- 音声解析・MIX推薦の中核ロジックはRust（`engine/crates/`）にのみ置き、FlutterやCLIへ重複実装しません。
- 設定値、単位、エラーコード、UI文言の定義元を明確にし、散在させません。

### SOLID原則

- **S（単一責任）**: モジュール・型・Widgetは一つの変更理由に限定します。解析、推薦、UI表示、入出力を混在させません。
- **O（開放/閉鎖）**: 新しい解析手法や推薦戦略は既存コードの改変ではなく、拡張ポイント（trait、インターフェース）で追加します。
- **L（リスコフ置換）**: 抽象型を利用する側は、具体実装の差し替えで挙動の前提が崩れないようにします。
- **I（インターフェース分離）**: 利用側が使わないAPIを持たせません。trait・抽象クラスは用途ごとに小さく分割します。
- **D（依存性逆転）**: 上位モジュールは下位の具体実装ではなく抽象に依存します。Flutterは解析エンジンの実装詳細ではなく、bridge経由の契約に依存します。

### 破壊的変更

- 本プロジェクトは初期開発段階であり、**後方互換性を維持する必要はありません**。
- 設計やAPIの改善のため、破壊的変更を躊躇せず行って構いません。
- 破壊的変更を行う場合は、影響範囲（Rust・Flutter・CLI・`contracts/`）を揃えて更新し、テストで整合性を確認します。
- 移行期間の互換レイヤーや非推奨警告は、明示的に求められない限り追加しません。

## Flutter / Dart 実装方針

- Dart SDK の要件、workspace、共通 lint はルートの `pubspec.yaml` と
  `analysis_options.yaml` を正とします。各 workspace は `resolution: workspace` を使います。
- 新しい Flutter アプリは `apps/` 配下に置き、機能単位で `lib/features/` を分割します。
  アプリ起動・依存の組み立て・ルーティングは機能実装から分離します。
- Widget は表示とユーザー操作の仲介に専念させます。音声解析、MIX 推薦、ファイル I/O、
  永続化などの中核処理を `build` 内へ置きません。
- 非同期 UI は、待機中、成功、空結果、失敗、必要ならキャンセル済みを区別して表現します。
- 解析結果は、進捗、信頼度、警告、失敗時にユーザーが取れる次の操作を表示できる型で扱います。
- ユーザーが修正した MIX 候補や区間は、解析の再実行で意図せず失わないようにします。
- 表示文言をロジックや Widget に重複して埋め込まず、ローカライズ可能な管理方法を採用します。
- 文字サイズ変更、十分なコントラスト、意味のあるラベル、キーボード・スクリーンリーダー操作を
  考慮します。
- 生成された FFI コードは手編集しません。生成元と生成手順を更新します。

## engine との境界

- 当面 `engine/`、その Rust API、生成設定は変更しません。クライアント側の必要が既存境界で
  満たせない場合は、代替 API をアプリに重複実装せず、変更案と必要な engine 側の対応を分けて
  提案します。
- `packages/mixup_ffi` は engine の公開機能を Dart / Flutter から利用する唯一の境界です。
  UI やアプリ機能からネイティブライブラリや生成された `src/rust/` を直接参照しません。
- FFI のエラーをそのままユーザー向け文言にせず、利用者が復旧できるアプリの状態とメッセージへ
  変換します。開発用の詳細は既存のログ機構に記録します。
- ネイティブライブラリの配置、モデル保存先、対応プラットフォームなどの実装固有の扱いは、
  アプリ固有の adapter に閉じ込めます。

## テストと検証

- バグ修正には、可能な限り再現テストを追加します。
- Dart / Flutter を変更したら、影響する workspace で次を実行します。

```sh
dart format --output=none --set-exit-if-changed <対象パス>
dart analyze <対象パス>
dart test <対象パス>
```

- Flutter アプリを追加した後は、対象アプリで `flutter analyze` と `flutter test` を実行します。
  プラットフォーム固有の変更では、対象プラットフォームでビルドまたは起動も確認します。
- package の公開 API や共有モデルを変更した場合は、その package に依存するアプリと package の
  テストも実行します。
- FFI 境界を変更した場合は、Dart 側の変換・エラー処理のテストを追加します。engine を変更する
  タスクでのみ、必要に応じて Rust 側の検証も実行します。
- ドキュメントのみの変更では、Dart / Flutter のテストは不要です。実行しなかった検証があれば
  理由と未検証範囲を最終報告に記載します。

## データと著作権

- 再配布権のない楽曲、録音、ユーザーが選択した音源をリポジトリへ追加しません。
- 音源パス、解析結果、個人情報をログやテスト出力へ不用意に出しません。
- 秘密情報、API キー、署名ファイル、大容量のモデルや生成物をコミットしません。
- テスト用音源や MIX データを追加する場合は、出所と利用条件を確認し、必要ならドキュメントへ
  記録します。

## 変更時のルール

- 未コミット変更はユーザーまたは別作業によるものとして扱い、依頼がない限り戻したり上書きしたり
  しません。
- 変更は依頼範囲に絞り、無関係なリファクタリング、整形、依存更新を混ぜません。
- 新規依存は最後の手段です。必要性、保守状況、ライセンス、対応プラットフォーム、workspace
  への影響を確認してから追加します。
- 公開 API、保存形式、設定、ルーティング、ユーザー操作に影響する変更では、関連する README
  または `docs/` を同じ変更で更新します。
- 自動生成物は生成元を変更して再生成し、生成物だけを直接修正しません。
- 実装前に類似する既存の package、画面、テストを検索し、既存パターンに合わせます。

## 完了条件

1. 変更が依頼範囲に収まり、既存のユーザー変更を損なっていない。
2. 主要な正常系と失敗系が、変更内容に見合うテストで確認されている。
3. 対象 workspace の整形、静的解析、テストが成功している。
4. package の公開境界とアプリ側の利用が整合している。
5. 音源、秘密情報、大容量モデルを誤って追加していない。
6. 実行できなかった確認がある場合は、理由と残るリスクを報告している。

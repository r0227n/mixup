# AGENTS.md

このファイルは、このリポジトリで作業するAIエージェントおよび開発者向けの共通ルールです。
プロジェクトの成長に合わせて、実際の構成・コマンド・判断基準と一致するよう更新してください。

## プロジェクト概要

楽曲を解析し、アイドルライブのMIXを入れやすい区間と適したMIXを提案し、楽曲に同期して練習できるアプリを開発します。

- GUIはFlutterで実装します。
- 音声解析とMIX推薦処理はRustで実装します。
- Rustの解析処理はFlutterとCLIの両方から再利用できるようにします。
- 自動解析は正解を断定せず、信頼度・推薦理由・注意事項を返します。

## 想定ディレクトリ構成

```text
.
├── app/                  # Flutterアプリ
├── engine/               # Rustワークスペース
│   ├── apps/
│   │   └── cli/          # CLIバイナリ
│   ├── crates/
│   │   ├── domain/       # 共通のドメイン型
│   │   ├── audio/        # デコード・正規化・リサンプリング
│   │   ├── analysis/     # 拍・小節・ボーカル・構成解析
│   │   ├── recommender/  # MIX候補抽出・照合・採点
│   │   └── bridge/       # FlutterとのFFI境界
│   ├── assets/           # エンジン開発用のローカル音源（Git管理外）
│   ├── models/           # モデルの取得情報とチェックサム
│   ├── scripts/          # エンジン開発補助スクリプト
│   └── tools/            # 解析結果の確認ツール
├── contracts/            # API・FFIで共有するデータ仕様
├── samples/              # 再配布可能なテスト用音源
└── docs/                 # 設計資料
```

実装されていないディレクトリを、将来必要になるという理由だけで先に作成しないでください。

## アーキテクチャ

依存関係は原則として次の方向に限定します。

```text
Flutter app -> bridge -> analysis / recommender
CLI -----------------> analysis / recommender
recommender ----------> analysis / domain
analysis -------------> audio / domain
audio ----------------> domain
```

- `domain`は他のプロジェクト内クレートへ依存させません。
- `cli`と`bridge`には音声解析や推薦の中核ロジックを書きません。
- FlutterにRust側の解析ロジックを重複実装しません。
- FFI固有の型をドメイン層へ持ち込みません。
- 外部ライブラリの型は境界でプロジェクト固有の型へ変換します。
- 循環依存を追加しないでください。

## 設計原則

Rust・Flutterを問わず、**SSOT**と**SOLID原則**に従って実装します。

### SSOT（Single Source of Truth）

- 仕様・型・定数・ビジネスルールは一箇所を正とし、他の場所へ複製しません。
- 共有スキーマは`contracts/`を正とし、Rust・Flutter・CLIはここから生成または参照します。
- 音声解析・MIX推薦の中核ロジックはRust（`engine/crates/`）にのみ置き、FlutterやCLIへ重複実装しません。
- 設定値、単位、エラーコード、UI文言の定義元を明確にし、散在させません。

### SOLID原則

- **S（単一責任）**: モジュール・型・Widgetは一つの変更理由に限定します。解析、推薦、UI表示、入出力を混在させません。
- **O（開放/閉鎖）**: 新しい解析手法や推薦戦略は既存コードの改変ではなく、拡張ポイント（trait、インターフェース）で追加します。
- **L（リスコフ置換）**: 抽象型を利用する側は、具体実装の差し替えで挙動の前提が崩れないようにします。
- **I（インターフェース分離）**: 利用側が使わないAPIを持たせません。trait・抽象クラスは用途ごとに小さく分割します。
- **D（依存性逆転）**: 上位モジュールは下位の具体実装ではなく抽象に依存します。Flutterは解析エンジンの実装詳細ではなく、bridge経由の契約に依存します。

### 破壊的変更

- 本プロジェクトは初期開発段階であり、**後方互換性を維持する必要はありません**。
- 設計やAPIの改善のため、破壊的変更を躊躇せず行って構いません。
- 破壊的変更を行う場合は、影響範囲（Rust・Flutter・CLI・`contracts/`）を揃えて更新し、テストで整合性を確認します。
- 移行期間の互換レイヤーや非推奨警告は、明示的に求められない限り追加しません。

## Rustの実装方針

- Rustワークスペースのルートは`engine/Cargo.toml`とします。
- 再利用可能な処理は`engine/crates/`へ置きます。
- CLIは`engine/apps/cli/`へ置き、バイナリ名は正式なアプリ名の決定後に統一します。
- 公開APIには、用途・単位・失敗条件が分かるドキュメントコメントを付けます。
- 時刻と長さの単位を型名またはフィールド名で明確にします。曖昧な数値だけを渡さないでください。
- 音声サンプルのチャンネル数、サンプルレート、値域をAPI境界で明示します。
- 解析失敗と低信頼度の解析結果を区別します。
- ライブラリ内で安易に`unwrap`または`expect`を使用しません。テストと到達不能が明白な箇所は例外です。
- CLIの人間向けメッセージは標準エラー出力、機械可読な結果は標準出力へ出します。
- CLIのJSON出力は、ログを混在させず安定したスキーマを維持します。

### Rustの基本確認

Rustプロジェクト作成後は、変更範囲に応じて以下を実行します。

```bash
cargo fmt --manifest-path engine/Cargo.toml --all --check
cargo check --manifest-path engine/Cargo.toml --workspace
cargo clippy --manifest-path engine/Cargo.toml --workspace --all-targets --all-features -- -D warnings
cargo test --manifest-path engine/Cargo.toml --workspace
```

開発中に依存クレートやネイティブライブラリの制約で`--all-features`が利用できない場合は、理由を記録し、実際にサポートするfeatureの組み合わせをテストしてください。

## Flutterの実装方針

- Flutterプロジェクトのルートは`app/`とします。
- `lib/features/`以下を機能単位で分割します。
- 共通Widgetは、複数機能で実際に利用されるまで`shared/`へ移動しません。
- Widgetに音声解析・推薦ロジックを書きません。
- 非同期状態は、待機中・成功・空結果・失敗を区別して表現します。
- ユーザーへ解析の進捗、信頼度、失敗理由を表示できるデータ構造にします。
- UI文言は将来のローカライズを妨げない形で管理します。
- アクセシビリティラベル、文字サイズ変更、十分なコントラストを考慮します。
- 生成されたFFIコードを手動編集しません。

### Flutterの基本確認

Flutterプロジェクト作成後は、変更範囲に応じて`app/`で以下を実行します。

```bash
dart format --output=none --set-exit-if-changed .
flutter analyze
flutter test
```

プラットフォーム固有の変更を行った場合は、対象プラットフォームでビルドまたは起動確認も行ってください。

## RustとFlutterの境界

- FFIには`flutter_rust_bridge`の利用を第一候補とします。ただし導入時点で要件と対応プラットフォームを再確認してください。
- 境界を越える型は単純でシリアライズ可能な構造を優先します。
- Rustの内部エラーやバックトレースを、そのままユーザー向けメッセージとして表示しません。
- 長時間の解析はUIスレッドをブロックせず、進捗通知とキャンセルを考慮します。
- 大きな音声バッファの不要なコピーを避けます。
- 生成コードの出力先と生成手順は`docs/`または`README.md`へ記録します。
- 生成コードをGit管理するかどうかは、CIで再現可能な生成環境を用意した時点で決定します。

## 音声解析と推薦結果

- 内部時刻の基準を統一し、外部出力では秒またはミリ秒を明記します。
- 拍、小節、セクションには開始時刻、終了時刻または長さ、信頼度を持たせます。
- 浮動小数点の時刻を同値比較しません。用途に応じた許容誤差を使います。
- 曲末の不完全な小節、無拍イントロ、テンポ変化、変拍子を想定します。
- MIX候補には、スコアだけでなく推薦理由と警告を含めます。
- グループや現場固有の慣習と、一般的な音響解析結果を混同しません。
- ユーザーによる修正を失わないデータモデルにします。
- アルゴリズム変更時に比較できるよう、解析器とモデルのバージョンを結果へ保存します。

## テスト方針

- バグ修正には、可能な限り再現テストを追加します。
- 音声解析のテストでは、短く再配布可能なfixtureまたはプログラム生成した信号を使用します。
- BPM、小節位置、推薦スコアなどの浮動小数点値には許容誤差を設定します。
- 正常系だけでなく、無音、短すぎる音源、破損ファイル、非対応形式をテストします。
- CLIは終了コード、標準出力、標準エラー出力をテストします。
- FFI境界は変換処理とエラー変換を個別にテストします。
- UIは重要なユーザーフローをWidgetテストまたはIntegration Testで保護します。
- 精度評価用データと通常の単体テストfixtureを分離します。

## データ・モデル・著作権

- 著作権上、再配布できない楽曲をリポジトリへコミットしません。
- ユーザー音源、録音、解析結果をログへ不用意に出力しません。
- 大きなモデルファイルを直接Gitへ追加しません。取得元、ライセンス、バージョン、チェックサムを`engine/models/`へ記録します。
- モデルやデータセットを追加する前に、商用利用・再配布・派生物の条件を確認します。
- 秘密情報、APIキー、署名ファイル、個人情報をコミットしません。
- サンプル音源には出所とライセンスを明記します。

## 変更時のルール

- 既存のユーザー変更を上書きまたは削除しません。
- タスクに関係しない大規模なリファクタリングを混ぜません。
- 新しい依存関係は、必要性、保守状況、ライセンス、対応プラットフォームを確認してから追加します。
- 公開スキーマ、CLI出力、FFI型の破壊的変更は問題ありません。変更時は影響範囲を揃えて更新し、テストで整合性を確認します。
- 仕様や構成を変更した場合は、関連する`README.md`または`docs/`も更新します。
- 自動生成物は生成元を変更し、生成物だけを直接修正しません。
- フォーマッターによる無関係な大量変更を避けます。

## 完了条件

変更を完了とする前に、少なくとも以下を確認してください。

1. 変更が依頼された範囲に収まっている。
2. 主要な正常系と失敗系がテストされている。
3. 対象言語のフォーマット・静的解析・テストが成功している。
4. RustとFlutterの境界を変更した場合、両側で整合性を確認している。
5. ユーザー音源、秘密情報、大容量モデルを誤って追加していない。
6. 実行できなかった確認がある場合、その理由と残るリスクを報告している。

## 未確定事項

以下は実装開始時に決定し、このファイルへ反映します。

- 正式なアプリ名とCLIバイナリ名
- 対応プラットフォーム
- FFIのみで構成するか、サーバーAPIも提供するか
- 状態管理・ルーティング・ローカルDBの採用技術
- 音声解析モデルと配布方法
- 解析結果スキーマのバージョニング方法
- 生成コードをGit管理するかどうか

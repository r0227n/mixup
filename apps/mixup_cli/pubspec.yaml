name: mixup_cli
description: Dart command-line application for the Mixup analysis engine.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  args: ^2.7.0
  mixup_ffi:
    path: ../../packages/mixup_ffi

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.3

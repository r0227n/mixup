name: mixup_ffi
description: Dart FFI bindings for installing Mixup models and analyzing audio.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  flutter_rust_bridge: 2.12.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.3

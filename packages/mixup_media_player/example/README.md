# mixup_media_player example

動画、音声、YouTube のネットワーク URL を切り替え、共通の `MixupMediaController` と
`MixupMediaViewer` で再生する example アプリです。

```sh
flutter run -d macos
```

画面内のプリセット URL は編集できます。再配布対象のメディアは example に同梱していません。
